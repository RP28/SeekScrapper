# SeekScrapper

An automated data pipeline to scrape and structure job-market data from SEEK, collecting 200+ listings per query.

Modular scraping and parsing components with resilient selectors and deduplication logic.

NLP techniques (TF-IDF + K-means) to cluster job descriptions and uncover hiring trends in AI, data engineering, and software roles.
